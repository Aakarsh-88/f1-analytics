# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Settings & validation
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Data processing (analytics service, CSV import)
pandas==2.2.3
numpy==1.26.4

# Caching
redis==5.2.1

# Auth (Clerk JWT verification)
python-jose[cryptography]==3.3.0
httpx==0.28.1

# CORS / middleware support (bundled with fastapi, listed for clarity)
starlette==0.41.3
